\documentclass[10pt]{article}
\usepackage[a4paper,margin=2.5cm]{geometry}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,booktabs,graphicx,microtype}
\usepackage[round]{natbib}
\usepackage[hidelinks]{hyperref}

\newcommand{\amortix}{\texttt{amortix}}

\title{\textbf{Amortized Bayesian parameter recovery for dynamical systems:\\
accuracy and cost against per-instance sampling}}
\author{Ivan Oseledets}
\date{draft}

\begin{document}
\maketitle

\begin{abstract}
% TODO: короткий безличный абстракт; главный месседж — один обученный на систему
% трансформер отвечает на p(m|S) для любого дизайна S за ~70 мс с точностью,
% сопоставимой с валидированными сэмплерами, которым нужны минуты на инстанс.
\end{abstract}

\section{Introduction}
% Мантра: (1) обратная задача динамических систем; (2) почему интересна: постериоры
% нужны повторно (калибровка, sequential design); (3) не решена: по-инстансный счёт
% дорог, амортизированные методы с fixed-length входами не дают p(m|S) для
% произвольных дизайнов; (4) идея: learnable-представление + дизайн-амортизация +
% референсы как запись. Our main contributions are: ...

\section{Method}
% Сжать из техрепорта: CFM + set-transformer, три фичи входа, рецепт (Muon,
% validated stopping). Питон-псевдокод из репорта.

\section{Benchmark systems and reference posteriors}
% Сьют 14 систем с уравнениями (перенос из репорта, компактнее);
% движки референсов и трёхгейтовая приёмка; кейс Heston как метод-находка.

\section{Comparison with per-instance sampling}
% ЯДРО СТАТЬИ. Таблица метод × система: dynesty / nautilus / tempered SMC /
% adaptive MH — расхождение с консенсусом (sd, FID), сек на постериор, вызовы logL;
% amortix строкой: FID к референсу, 70 мс, час обучения. Кривая цена-качество,
% брейк-эвены. Пилотные числа nautilus в PLAN.md.

\section{Design amortization}
% p(m|S) для любого S: Fig fid-vs-K, сравнение с single-design сетями.

\section{Related work}
% NPE/FMPE/BayesFlow/Simformer — как related work, без сравнения точности
% (fixed-length интерфейсы; наша постановка вне их области применимости).

\section{Discussion}

\bibliographystyle{plainnat}
% \bibliography{...}
\end{document}
